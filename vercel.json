{
  "version": 2,
  "outputDirectory": "dist",
  "buildCommand": "npm --prefix bharatos-app install && npm --prefix bharatos-app run build && node sync_dist.js",
  "cleanUrls": true,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}